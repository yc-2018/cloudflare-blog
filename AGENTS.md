# AGENTS.md

本文件为本项目中的 AI 编码代理协作指南。若本文档与实际代码不一致，以实际代码为准，并在合适时更新文档。

## 项目背景

这是一个部署到 Cloudflare 免费服务的个人博客项目：

- 前端：Vite + React + TypeScript
- 部署：Cloudflare Pages
- 后端：Cloudflare Pages Functions
- 数据库：Cloudflare D1
- Markdown：GitHub 风格 Markdown 渲染与代码高亮

## 项目优先规则

- 优先遵循项目现有风格、架构和测试方式。
- 生成代码必须使用 UTF-8 编码。
- 编写类或方法时都要写注释，说明用途、关键约束或行为。
- 生成的一些变量也要在变量尾部加注释，说明变量含义。
- 编写完成后检查中文，确认中文没有全部变成问号，也没有变成 Unicode 转义。
- 提供 URL 链接时，必须先验证链接的有效性。
- 如果文档与实际代码不一致，以实际代码为准，并在合适时更新文档。

## 开发与验证

常用命令：

```bash
pnpm install
pnpm build
pnpm test
pnpm typecheck:functions
pnpm db:migrate:local
pnpm db:seed:local
pnpm cf:dev
```

本地预览通常运行在：

```text
http://127.0.0.1:8799/
```

修改后优先运行：

```bash
pnpm build
pnpm test
```

涉及 Pages Functions 时额外运行：

```bash
pnpm typecheck:functions
```

## 交付与推送流程

- 每次完成代码修改并通过必要的构建、测试和类型检查后，必须启动本地服务供用户检测。
- 默认使用 `pnpm cf:dev`，并向用户提供可访问的本地 URL（通常为 `http://127.0.0.1:8799/`）。端口被占用时改用其他端口，并告知实际 URL。
- 本地服务启动后保持运行，等待用户完成检测；用户未明确确认前，不执行远程推送、远程数据库迁移或其他发布操作。
- 只有用户明确说“推送”或等价确认后，才检查 Git 差异并推送代码修改。禁止擅自强制推送、重置或覆盖用户已有改动。
- 如果本次修改包含 D1 migration 文件，用户确认推送后还要执行 `pnpm db:migrate:remote`，将数据库修改应用到远程 D1；没有 migration 修改时不要执行该命令。
- 推送前必须向用户说明将要推送的代码文件和数据库 migration；代码推送与远程数据库迁移都完成后，再报告结果和可能的部署状态。

## 编码注意事项

- 不要无故改变现有架构。
- 不要为了单次使用添加抽象。
- 不要扩大修改范围。
- 不要改动无关格式、注释或样式。
- 不要删除用户或其他代理已有改动，除非明确要求。
- 对用户可见的中文文本，修改后要检查编码显示。
- 对 Markdown、登录、权限、D1 查询等核心路径，修改后要做实际验证。

## 翻译附录：CLAUDE.md

以下内容翻译自：

https://raw.githubusercontent.com/forrestchang/andrej-karpathy-skills/main/CLAUDE.md

该链接已在 2026-05-20 验证可访问。

# CLAUDE.md

用于减少常见 LLM 编码错误的行为指南。可按需要与项目专用说明合并。

**权衡：** 这些指南偏向谨慎而不是速度。对于非常简单的任务，请自行判断。

## 1. 编码前先思考

**不要假设。不要掩盖困惑。要说明取舍。**

实现前：

- 明确说明你的假设。如果不确定，就提问。
- 如果存在多种理解方式，要把它们说出来，不要悄悄选择其中一种。
- 如果存在更简单的做法，要说明。必要时要提出反对意见。
- 如果某件事不清楚，就停下来。说明哪里令人困惑，并提问。

## 2. 简单优先

**用能解决问题的最少代码。不要做猜测性的扩展。**

- 不要添加超出请求范围的功能。
- 不要为只使用一次的代码添加抽象。
- 不要添加未被请求的“灵活性”或“可配置性”。
- 不要为不可能发生的场景写错误处理。
- 如果你写了 200 行，但其实 50 行就能完成，请重写。

问问自己：“资深工程师会不会觉得这太复杂了？”如果答案是会，就简化。

## 3. 外科手术式修改

**只改必须改的地方。只清理自己造成的问题。**

编辑现有代码时：

- 不要“顺手改进”相邻代码、注释或格式。
- 不要重构没有坏掉的东西。
- 匹配现有风格，即使你会用不同方式实现。
- 如果发现无关的死代码，可以提及，但不要删除。

当你的修改造成孤立代码时：

- 删除由你的修改导致不再使用的 import、变量或函数。
- 不要删除修改前就已经存在的死代码，除非用户要求。

检验标准：每一行变更都应能直接追溯到用户请求。

## 4. 目标驱动执行

**定义成功标准。循环推进，直到验证通过。**

把任务转化为可验证目标：

- “添加校验” → “为非法输入编写测试，然后让测试通过”
- “修复 bug” → “编写能复现 bug 的测试，然后让测试通过”
- “重构 X” → “确保重构前后测试都通过”

对于多步骤任务，先写一个简短计划：

```text
1. [步骤] → 验证：[检查]
2. [步骤] → 验证：[检查]
3. [步骤] → 验证：[检查]
```

强成功标准能让你独立循环推进。弱标准，例如“让它能工作”，会导致不断需要澄清。

---

**如果这些指南有效，你会看到：** diff 中不必要变更更少，因为过度复杂导致的重写更少，澄清问题会出现在实现前，而不是错误发生后。
